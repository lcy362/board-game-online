name = "speed101-games"
pages_build_output_dir = "dist"
# 一键部署说明：
#   本地：npm install && npm run deploy   （需先 npx wrangler login）
#   Git 自动部署：在 Cloudflare 控制台 Pages 连接本仓库，
#   构建命令填 `npm run build`，输出目录填 `dist` 即可。
#   自定义域名 speed101.games 在控制台 Pages → Custom domains 绑定。
